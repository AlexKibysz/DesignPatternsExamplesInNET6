namespace PatronesCreacionales
{
    internal class Builder
    {
        //PEGAR EN PROGRAM.CS

        /*
        IBuilderPedidoOnline builderPedidoOnline = new BuilderPedidoOnline();

        DirectorPedido directorPedido = new DirectorPedido(builderPedidoOnline);

        //public void ConstruirPedido(int id, DateTime fecha, string call, List<Articulos> articulos)

        int id = RandomNumberGenerator.GetInt32(199);
        DateTime dateTime = DateTime.Now;
        Console.WriteLine("INGRESE: CALLE");
        string calle = "Magallanes";
        Console.WriteLine("INGRESE: COD POSTAL");
        int codigoPostal = 123;
        Console.WriteLine("INGRESE: PROVINCIA");
        string provincia = "Cordoba";

        List<Articulos> articulos = new List<Articulos>();

        Console.WriteLine("------CREAR ARTICULOS-----");

        for (int i = 0; i<RandomNumberGenerator.GetInt32(10); i++)
        {
            Articulos articulo = new Articulos(RandomNumberGenerator.GetInt32(20), "TEST" + i.ToString(), "descripcion generica", RandomNumberGenerator.GetInt32(21023));
        articulos.Add(articulo);
        }

    DireccionPedido direccionPedido = new DireccionPedido(calle, codigoPostal, provincia);

    directorPedido.ConstruirPedido(id, dateTime, direccionPedido, articulos);
        */
    }
}