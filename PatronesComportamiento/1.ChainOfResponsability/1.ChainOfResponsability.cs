namespace PatronesComportamiento
{
    internal class ChainOfResponsability
    {
        /*using PatronesComportamiento._1.ChainOfResponsability;
using PatronesComportamiento._1.ChainOfResponsability.ConcreteHandlers;

class Program
{
    static async Task Main(string[] args)
    {
        // Creación de la cadena de manejadores
        var stockHandler = new StockDisponibleHandler();
        var paymentHandler = new paymentHandler();
        var discountHandler = new DiscountHandler();

        stockHandler.SetNext(paymentHandler).SetNext(discountHandler);

        // Creación de una orden de compra de ejemplo
        var order = new Order { ammount = 3, cost = 34, disponible = true, id = 1 };

        // Procesamiento de la orden a través de la cadena
        bool result = await stockHandler.HandleAsync(order);
        Console.WriteLine(result ? $"Orden procesada con éxito. Precio final: {order.cost:C}" : "Orden rechazada.");
    }
}*/
    }
}